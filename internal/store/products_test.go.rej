@@ -13,11 +13,12 @@
 	ctx := setupTestDB(t)
 
 	// Create a product first
+	prodID := NewTestID()
 	product, err := store.CreateProduct(ctx, db.CreateProductParams{
-		ID:        NewTestID(),
+		ID:        prodID,
 		Name:      "Test Product",
 		Desc:      "Test Description",
-		Slug:      "test-product",
+		Slug:      "test-product-" + prodID,
 		Amount:    "99.99",
 		Metadata:  []byte("{}"),
 		Attribute: []byte("{}"),
@@ -25,15 +26,15 @@
 		Active:    true,
 	})
 	require.NoError(t, err)
-	require.Equal(t, NewTestID(), product.ID)
+	require.Equal(t, prodID, product.ID)
 
 	// Retrieve product by ID
-	retrieved, err := store.GetProductByID(ctx, NewTestID())
+	retrieved, err := store.GetProductByID(ctx, prodID)
 	require.NoError(t, err)
-	require.Equal(t, NewTestID(), retrieved.ID)
+	require.Equal(t, prodID, retrieved.ID)
 	require.Equal(t, "Test Product", retrieved.Name)
 	require.Equal(t, "Test Description", retrieved.Desc)
-	require.Equal(t, "test-product", retrieved.Slug)
+	require.Equal(t, "test-product-" + prodID, retrieved.Slug)
 	require.Equal(t, "99.99", retrieved.Amount)
 }
 
